def print_my_name():
    name = "Rume"
    print(f"My name is {name}") # string interpolation or format string

if __name__ == "__main__":
    print_my_name()

    # A repository is a project. There are different branches (features) in a repository
    # Developers commit their work into branches and from there it is pushed into github (cloud)
    # Github merges (merging) all the branches together and a piece of working software is created
    #  In git there is a master branch called main